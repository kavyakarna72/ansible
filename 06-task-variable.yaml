- name: variables
  hosts: local
  connection: local
  vars: # PLAY LEVEL VARIABLE,global variables
    COURSE: "DevOps with AWS"
    DURATION: 120HRS
    TRAINER: Kavya
  tasks:
  - name: print the variable
    vars: # task level variable, accessible onle with in the task. task variable can override play level variable.Local variable
      COURSE: Ansible
    ansible.builtin.debug:
      msg: "course is{{ COURSE}}, Duration is{{ DURATION}}, Trainer is {{ TRAINER}}"

  - name: print the variable1
    ansible.builtin.debug:
    msg: "course is{{ COURSE}}, Duration is{{ DURATION}}, Trainer is {{ TRAINER}}"
    